# Python dependencies for the prototype.
# Versions are pinned loosely (~=) to allow patch updates but not breaking
# changes. Locked exact versions live in requirements-lock.txt once we run
# `pip-compile` for the first time on Day 2.

# Web framework
fastapi ~= 0.115
uvicorn[standard] ~= 0.32

# HTTP client (async-capable)
httpx ~= 0.27

# Data layer
sqlalchemy ~= 2.0

# Config
python-dotenv ~= 1.0
pydantic-settings ~= 2.6

# Templates
jinja2 ~= 3.1

# Testing (development)
pytest ~= 8.3
pytest-asyncio ~= 0.24
